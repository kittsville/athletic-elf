services:
  db:
    image: postgres:17
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: strava
      POSTGRES_PASSWORD: strava
      POSTGRES_DB: strava
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
