export CLIENT_ID=111111
export CLIENT_SECRET=sdkjeirjfe83ied8wokdw9d9q
export VERIFY_TOKEN=SPECIAL_TOKEN
# Set to 0 for local http:// dev. Omitted or true when FLASK_ENV=production: HTTP requests get 403.
export ENFORCE_HTTPS=0
export DOMAIN=https://your-app.example
export APP_DEVELOPER_IDS=11111,22222,33333
# Required (validated at startup). Competition window and period boundaries for team scoring.
export COMPETITION_START_DATETIME=2026-04-01T00:00:00+00:00
# Comma-separated ISO 8601 instants (UTC or offsets). Each is the exclusive end of a scoring
# period (activity start_date must be < boundary). Merged with COMPETITION_END_DATETIME. May be omitted (empty) for a single period from start to end.
export WEEK_BOUNDARIES=2026-05-11T00:00:00+00:00,2026-05-18T00:00:00+00:00
export COMPETITION_END_DATETIME=2026-05-31T23:59:59+00:00
export HUB_OPTIONS=London,Aberdeen,Manchester
export DEPARTMENT_OPTIONS="Engineering,Product,HR"
export APP_NAME="My Sporting App"
export APP_FAVICON="https://your-app.example/favicon.ico"
